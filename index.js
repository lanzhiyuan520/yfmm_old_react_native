import { AppRegistry,Platform,Dimensions,PixelRatio } from 'react-native';
import React, { Component } from 'react';
import Router from "./router";
AppRegistry.registerComponent('AwesomeProject', () => Router);